<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Book Car</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <div class="auth-card">
    <a href="/cars" class="btn btn-outline" style="margin-bottom:12px;">← Back</a>
    <h1 style="margin:0 0 12px; font-size:22px; font-weight:800;">Book: <%= car.model %></h1>

    <img src="<%= car.imageUrl %>" alt="<%= car.model %>"
         style="width:100%; height:200px; object-fit:cover; border-radius:10px; margin-bottom:12px;" />

    <form action="/book" method="post">
      <input type="hidden" name="carId" value="<%= car._id %>">

      <div class="form-group">
        <label for="returnDate">Return Date</label>
        <input id="returnDate" name="returnDate" type="date" required />
      </div>

      <button type="submit" class="btn btn-primary" style="width:100%;">Confirm Booking</button>
    </form>
  </div>
</body>
</html>
